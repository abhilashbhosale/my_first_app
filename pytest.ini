# pytest.ini
[pytest]
# Tell pytest to look for test files inside the 'tests' folder
testpaths = tests
# Standard naming convention for test files (already correct, but good to include)
python_files = test_*.py